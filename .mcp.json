{
  "mcpServers": {
    "figma": {
      "command": "bash",
      "args": [
        "-c",
        "set -a && source /home/user/figma-mcp/.env && set +a && npx -y @figma/mcp-server-figma"
      ]
    }
  }
}
